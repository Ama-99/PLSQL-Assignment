CREATE OR REPLACE VIEW AMA_BRANCH_AVILABLE_CAPACITY_view AS
SELECT BRANCH_ID,
		BRANCH_NAME,
		TOTAL_WAREHOUSE_CAPACITY,
    AMA_BRANCH_API.GET_REMAINING_CAPACITY(Branch_Id) AS available_capacity
FROM AMA_BRANCH_TAB;
